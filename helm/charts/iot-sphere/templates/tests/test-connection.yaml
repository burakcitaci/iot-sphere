apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iot-sphere.fullname" . }}-test-connection"
  labels:
    {{- include "iot-sphere.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "iot-sphere.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
