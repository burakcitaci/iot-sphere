apiVersion: v1
kind: Service
metadata:
  name: {{ include "iot-sphere.fullname" . }}
  labels:
    {{- include "iot-sphere.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iot-sphere.selectorLabels" . | nindent 4 }}
